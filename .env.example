# SafeAlert Rwanda - Environment Variables Template
# Copy this file to .env.local (for development) or set these in your deployment platform

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
# Your Neon PostgreSQL database connection string
# Get this from: https://console.neon.tech/
DATABASE_URL=postgresql://username:password@hostname:port/database?sslmode=require

# =============================================================================
# AUTHENTICATION (OPTIONAL - if using Stack Auth)
# =============================================================================
# Stack Auth configuration
# Get these from: https://app.stack-auth.com/
NEXT_PUBLIC_STACK_PROJECT_ID=your_project_id
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=your_publishable_key
STACK_SECRET_SERVER_KEY=your_secret_key

# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# Set to 'production' for production deployment
NODE_ENV=development

# Custom ping message for API testing
PING_MESSAGE=SafeAlert Rwanda API is running

# =============================================================================
# EXTERNAL SERVICES (OPTIONAL)
# =============================================================================
# If you want to integrate with external SMS services
# SMS_API_KEY=your_sms_api_key
# SMS_API_URL=your_sms_api_url

# If you want to integrate with mapping services
# MAPBOX_API_KEY=your_mapbox_key
# GOOGLE_MAPS_API_KEY=your_google_maps_key

# =============================================================================
# SECURITY (PRODUCTION ONLY)
# =============================================================================
# CORS origins for production (comma-separated)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Session secret for production
# SESSION_SECRET=your_very_secure_random_string_here

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# For Netlify: Set these in Site Settings > Environment Variables
# For Vercel: Set these in Project Settings > Environment Variables
# For local development: Copy this file to .env.local
